// import { Catalog, Basket, sumBasket } from "./js/lists";
import {appBasket, appCatalog} from "./js/app";

export default () => {
    // let basket = new Basket('/getBasket.json', '#basket' );
    // basket.openCart()
    // let catalog = new Catalog('/catalogData.json', '#index_catalog', basket);
    let basket = appBasket;
    let catalog = appCatalog;
}
